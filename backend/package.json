{
  "name": "backend",
  "version": "1.0.0",
  "description": "Rihan Fashion backend scaffold",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "seed": "node seed.js",
    "seed:reset": "node -e \"require('./models/Dress').deleteMany({}).then(()=>process.exit(0))\"",
    "prod": "NODE_ENV=production node index.js",
    "build": "echo 'Minification: use a tool like html-minifier, clean-css, uglify-js to minify frontend files before deployment.'"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "express-rate-limit": "^7.5.1",
    "helmet": "^7.0.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^7.0.0",
    "multer": "^2.1.1"
  }
}
